# No extra pip packages are required beyond the ComfyUI bundle venv.
# Uses ComfyUI's torch/numpy/Pillow stack, a working FFmpeg (imageio-ffmpeg or VideoHelperSuite)
# and, for lyric timing in the v1.2.2 planner, openai-whisper (already part of the bundle venv).
